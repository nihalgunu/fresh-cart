apiVersion: v1
kind: Service
metadata:
  name: notification-db
  namespace: ecommerce-data
spec:
  clusterIP: None
  selector:
    app: notification-db
  ports:
    - port: 27017
      targetPort: 27017
