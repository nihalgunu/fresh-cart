apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: observability-allow-all
  namespace: observability
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from: []
